# ASGARD 下一阶段主线计划

## 1. 当前基线

- `mmg2` 路线已正式放弃，不再作为主线继续维护或验收。
- `FS_electron_slc1` 的真实主线重新收束到 `slc1`。
- 固定构建入口保持不变：
  - `python build_extensions.py --module FS_electron_slc1 --force`
- 固定阶数诊断入口保持不变：
  - `python tests/order_convergence_check.py`
- 禁止再添加任何会复用旧结果的缓存实现，尤其是诊断链、构建跳过和运行态状态复用。

## 2. 当前真实问题

- `slc1` 的低能端电子谱相对 `fullhide` 被过早截断。
- 这个问题可以直接从 compare 图和壳层谱对照里看到：
  - 早时刻与中早时刻壳层的 `g_lo` 明显偏高；
  - 低能冷却尾部没有平滑延伸到应有位置。
- 当前优先怀疑点是 `slc1` 半拉格朗日输运的左边界口径，而不是辐射后处理层。

## 3. 主线任务

### 3.1 数值层

- 只继续优化 `slc1`。
- 直接检查并修正低能端输运、源项更新、左边界沉积口径。
- 不通过非数值保护绕过问题。
- 任何新的参数图或时空演化率若不连续、不平滑，视为错误实现。

### 3.2 代码清理

- 删除 `mmg2` 的电子谱求解入口和公开运行路径。
- 保留仍被辐射核或其共享公共例程调用的代码。
- 同步清理与 `mmg2` 绑定的测试、文档和基准脚本，避免后续诊断继续混入已放弃路线。

## 4. 执行顺序

1. 先清掉 `mmg2` 的运行入口、导出和主测试口径。
2. 再修 `slc1` 低能端过早截断。
3. 每轮数值修改后只做最小相关检查：
   - `python build_extensions.py --module FS_electron_slc1 --force`
   - `python tests/order_convergence_check.py`
4. 如有必要，补画 `fullhide vs slc1` 的电子谱 compare 图，只看低能尾部是否连续平滑且向参考解收敛。

## 5. Done When

- 正式验收口径下阶数满足要求。
- `slc1` 低能端电子谱不再出现过早截断。
- 对照图中的低能尾部随壳层和时间演化保持连续、平滑、物理上合理。
