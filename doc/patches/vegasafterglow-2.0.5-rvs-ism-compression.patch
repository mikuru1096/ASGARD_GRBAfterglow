diff --git a/src/dynamics/reverse-shock.tpp b/src/dynamics/reverse-shock.tpp
index 0000000..0000000 100644
--- a/src/dynamics/reverse-shock.tpp
+++ b/src/dynamics/reverse-shock.tpp
@@ -137 +137,4 @@
-    Real dx3dt = (beta4 - beta3) * con::c / ((1 - beta3) * (state.Gamma * comp_ratio / this->Gamma4 - 1));
+    const Real compression_advance = state.Gamma * comp_ratio / this->Gamma4 - 1;
+    if (compression_advance <= 0)
+        return spreading;
+    Real dx3dt = (beta4 - beta3) * con::c / ((1 - beta3) * compression_advance);
@@ -155,0 +159,3 @@
+    if (state.Gamma * comp_ratio / this->Gamma4 <= 1)
+        return 0.;
+
diff --git a/src/config/simulation-defaults.h b/src/config/simulation-defaults.h
index 0000000..0000000 100644
--- a/src/config/simulation-defaults.h
+++ b/src/config/simulation-defaults.h
@@ -97 +97 @@
-        inline constexpr std::size_t max_ode_steps = 100000;
+        inline constexpr std::size_t max_ode_steps = 1000000;
