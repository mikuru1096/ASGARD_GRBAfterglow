# 辐射偏振度计算物理核心升级计划

## Summary
- 新增 `Model.polarization(times_s, nu_hz, *, magnetic_geometry="shock_random", local_emissivity="analytic_then_kernel")`，返回同步辐射 Stokes 结果：`I, Q, U, linear_polarization, polarization_angle_rad`。
- 第一覆盖范围：FS 电子同步、RS 电子同步、强子带电粒子同步，包括 proton/pion/muon synch；SSC、IC、pγ/pp 衰变 γ 暂不计入偏振 Stokes。
- 磁场几何支持两种：`shock_random` 和 `toroidal`。PA 约定为天球基底 `sky_x -> sky_y` 的弧度角，`PA = 0.5 * arctan2(U, Q)`。

## Task Status
- [x] `Model.polarization(times_s, nu_hz, *, magnetic_geometry="shock_random", local_emissivity="analytic_then_kernel")` 已实现。
- [x] `PolarizationResult` dataclass 已实现并导出，字段为 `I_sync, Q, U, linear_polarization, polarization_angle_rad, components`。
- [x] Python patch-grid Stokes 后端已实现，覆盖 FS electron synch、RS electron synch、FS hadronic proton/pion/muon synch、RS hadronic proton synch。
- [x] `shock_random` 与 `toroidal` 磁场几何已接入。
- [x] Fortran 输出专用频率依赖偏振核已新增：electron 与 hadronic；当前由数值粒子谱局域斜率加同步发射权重得到 `Pi_nu`，并保持 split 后强度守恒测试。
- [x] `tests/polarization_smoke.py` 已新增，覆盖 on-axis、off-axis、toroidal、FS/RS/hadronic、核强度守恒与频率依赖 smoke。
- [x] 已通过检查：`py_compile`、`git diff --check`、`tests/polarization_smoke.py`、`tests/hadronic_reverse_shock_smoke.py`，以及此前相关 Fortran force build、line-truncation、smoke。
- [ ] 完整物理的 `j_perp` / `j_parallel` Bessel emissivity 核积分尚未完成；需要替换当前基于局域谱斜率的 `Pi_nu` 近似，并继续保证 `j_perp + j_parallel` 回归原同步总强度。
- [ ] 后续性能优化：按需要将更多 patch-grid Stokes 编排下沉到 Fortran，但不得改变现有物理定义和 PA 约定。

## Key Changes
- API 层新增 `PolarizationResult` dataclass，字段固定为：
  `I_sync, Q, U, linear_polarization, polarization_angle_rad, components`。
- `Model.polarization(...)` 使用现有 patch 角网格编排，不走直接标量 `sed_interpolation`，因为标量核丢失每个角元的偏振角。
- 第一实现步骤先用幂律同步局域偏振率：
  `Pi = (p + 1) / (p + 7/3)`，只负责把几何 Stokes 积分做正确。
- 第二实现步骤立即升级频率依赖同步核：
  在 Fortran 辐射核中新增偏振同步 emissivity 计算，积分 `j_perp` / `j_parallel`，由数值粒子谱得到局域 `Pi(nu, shell)`，替换解析常数 Pi。
- 对非同步分支不做“兜底偏振”：不把 IC/SSC/衰变 γ 伪装成零偏振后混入 `Pi_sync`；如后续需要观测总偏振稀释，另加显式参数。

## Implementation Details
- Python 角网格后端复用 `_iter_patches`、`_solve_patch_state`、`_observe_parts` 的现有路径；每个 patch 单独得到同步强度，然后按局域 PA 累加：
  `Q += Pi * I * cos(2PA)`，`U += Pi * I * sin(2PA)`。
- `shock_random`：采用冲击面二维随机场的标准局域净偏振方向，由 patch 速度方向、视线方向和 sky basis 决定；轴对称 on-axis 情况数值上应给出 `Q=U=0`。
- `toroidal`：用喷流轴定义全局环向磁场方向，将其投影到天球面后得到 PA；只在轴对称喷流几何内支持。
- 强子同步只包含带电粒子同步分支：proton synch、pion synch、muon synch；neutrino、pγ/pp 直接 γ、BH/IC 分支不进入偏振 Stokes。
- Fortran 频率依赖核完成后必须保持总强度与现有未偏振同步 luminosity 一致：`j_perp + j_parallel` 回归到原 `l_syn_spec`。

## Test Plan
- 编译检查：
  `FS_electron_fullhide_1d`、`FS_electron_fullhide_2d`、`FS_hadronic_1d`，并对受影响 Fortran 源跑 `-Wline-truncation`。
- 单元/物理测试：
  on-axis tophat + `shock_random` 得到 `linear_polarization ~= 0`。
  off-axis tophat 得到连续平滑的 `Pi(t)` 和 `PA(t)`。
  `Q,U` 旋转变换满足 PA 约定。
  `toroidal` 与 `shock_random` 在同一模型下给出不同但连续的 PA 演化。
- 核一致性测试：
  频率依赖偏振核的 `j_perp + j_parallel` 与原同步总强度相对误差在严格阈值内。
  幂律电子谱下局域 `Pi(nu)` 在 optically thin 区域回到 `(p+1)/(p+7/3)`。
- smoke：
  新增 `tests/polarization_smoke.py`，覆盖 FS、RS、hadronic synch 三条路径。
  重要改动后跑最小相关 smoke 和基准光变，检查偏振度和 PA 随时间/频率平滑。

## Assumptions
- 第一版偏振度定义为“同步辐射偏振度”，不是包含 SSC/IC/衰变 γ 稀释后的总观测偏振度。
- Python 后端是正式角向 Stokes 积分实现，不是降级路径；Fortran 化只作为后续性能优化和频率依赖 emissivity 核升级。
- 不添加数值保护或后处理平滑；若 `Pi(t,nu)` 或 PA 出现非物理跳变，按 bug 处理。
